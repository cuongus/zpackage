projection;
strict ( 2 );
use draft;

define behavior for ZC_CORE_TB_TEMPPDF alias CORE_TB_TEMPPDF
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}