projection;
strict ( 2 );
use draft;

define behavior for ZC_TBYEAR //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
  use action CreateYearFrTo;
  use action CreateYearFrToS;
  use association _dtl { create; with draft; }
}

define behavior for ZC_TBPERIOD //alias <alias_name>
{
  use update;
  use delete;

  use association _hdr { with draft; }
}