projection implementation in class ZBP_C_TB_MALOI_HANG unique;
strict ( 2 );
use draft;
define behavior for ZC_TB_MALOI_HANG alias ZcTbMaloiHang
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use association _dtl { create; with draft; }
}

define behavior for ZC_TB_LOI_H_DTL //alias <alias_name>
use etag
{
  use update;
  use delete;

  use association _hdr { with draft; }
}